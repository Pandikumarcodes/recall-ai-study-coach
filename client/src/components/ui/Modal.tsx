import { AnimatePresence, motion } from "framer-motion";
import { X } from "lucide-react";
import type { ReactNode } from "react";
interface ModalProps { children: ReactNode; onClose: () => void; open: boolean; title: string; }
export function Modal({ children, onClose, open, title }: ModalProps) { return <AnimatePresence>{open && <><motion.button aria-label="Close modal" className="fixed inset-0 z-40 bg-text-primary/35 backdrop-blur-[1px]" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} onClick={onClose} type="button" /><motion.section aria-label={title} aria-modal="true" className="fixed left-1/2 top-1/2 z-50 w-[calc(100%-2rem)] max-w-lg -translate-x-1/2 -translate-y-1/2 rounded-2xl border border-border bg-surface p-5 shadow-card-hover sm:p-6" initial={{ opacity: 0, scale: 0.96, y: 12 }} animate={{ opacity: 1, scale: 1, y: 0 }} exit={{ opacity: 0, scale: 0.96, y: 12 }}><div className="flex items-center justify-between gap-4"><h2 className="text-xl font-bold text-text-primary">{title}</h2><button aria-label="Close modal" className="grid size-9 place-items-center rounded-xl text-text-secondary hover:bg-surface-muted" onClick={onClose} type="button"><X size={19} /></button></div><div className="mt-5">{children}</div></motion.section></>}</AnimatePresence>; }
