import { motion } from "framer-motion";
import { Award, BrainCircuit, Flame, LockKeyhole, Medal } from "lucide-react";
const achievements = [{ icon: Flame, title: "12 Day Streak", unlocked: true }, { icon: Medal, title: "Quiz Champion", unlocked: true }, { icon: BrainCircuit, title: "Recall Master", unlocked: true }, { icon: Award, title: "Note Taker", unlocked: false }];
export function AchievementGallery() { return <section className="rounded-2xl border border-border bg-surface p-5 shadow-card"><h2 className="text-lg font-bold text-text-primary">Achievement Gallery</h2><div className="mt-5 grid grid-cols-2 gap-3 sm:grid-cols-4">{achievements.map((achievement) => { const Icon = achievement.unlocked ? achievement.icon : LockKeyhole; return <motion.div className={`rounded-xl p-4 text-center ${achievement.unlocked ? "bg-surface-muted" : "bg-surface-muted/60 opacity-60"}`} key={achievement.title} whileHover={achievement.unlocked ? { y: -4, scale: 1.02 } : undefined}><span className={`mx-auto grid size-10 place-items-center rounded-xl ${achievement.unlocked ? "bg-primary/10 text-primary" : "bg-border text-text-secondary"}`}><Icon size={20} /></span><p className="mt-3 text-xs font-bold text-text-primary">{achievement.title}</p><p className="mt-1 text-[11px] text-text-secondary">{achievement.unlocked ? "Unlocked" : "Locked"}</p></motion.div>; })}</div></section>; }
